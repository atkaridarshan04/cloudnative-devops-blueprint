🚀 MERN Stack Application Successfully Deployed!
================================================================================

📋 DEPLOYMENT SUMMARY:
  Release Name:    {{ .Release.Name }}
  Namespace:       {{ .Values.namespace }}
  Chart Version:   {{ .Chart.Version }}
  App Version:     {{ .Chart.AppVersion }}

🌐 APPLICATION ACCESS:
  Served via Gateway API (Envoy Gateway), not Ingress — TLS terminated at the Gateway
  with a Let's Encrypt cert (see gateway/certificate.yml):
  🔗 https://{{ .Values.gateway.hostname }}/

IMPORTANT NOTES:
  - MongoDB has no persistent storage (ephemeral) — data is lost on Pod restart
  - Default credentials are stored in Secrets
  - Health checks are configured for all services
  - frontend/backend deploy as Argo Rollouts (canary), not plain Deployments —
    see helm-chart/templates/backend.yaml / frontend.yaml

📖 For more information, visit: https://github.com/atkaridarshan04/CloudNative-DevOps-Blueprint/

Happy Helming!
