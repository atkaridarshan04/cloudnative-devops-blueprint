apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - mongodb.yml
  - httproute.yml
  - backend.yml
  - frontend.yml
  # Disabled for now while debugging the PSA/istio-sidecar conflict in namespace.yml.
  # - network-policies.yml
# mongodb creds come from the mongodb-credentials-external Secret (ExternalSecret, synced
# from Vault) — each overlay's external-secret.yml, not a secretGenerator here.
