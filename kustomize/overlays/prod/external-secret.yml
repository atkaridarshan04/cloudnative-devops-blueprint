apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mongodb-external-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: mongodb-credentials-external # unprefixed — namespace isolation (dev/staging/prod) already prevents collision
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        MONGODB_URL: "mongodb://{{ .username }}:{{ .password }}@prod-mongodb-deployment-0.prod-mongodb-service-headless.prod.svc.cluster.local:27017/db?authSource=admin"
        MONGODB_USER: "{{ .username }}"
        MONGODB_PASSWORD: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: prod/mern-backend/mongodb
        property: username
    - secretKey: password
      remoteRef:
        key: prod/mern-backend/mongodb
        property: password
